node_modules/
dist/
coverage/
*.log

# Real-run working trees (the daemon clones each target repo into <workingRoot>/run-<id>)
.agent-work/

# Local SQLite databases
*.db
*.db-shm
*.db-wal

# Environment / secrets (see README §3.3 Layer 5)
.env
.env.*
!.env.example
